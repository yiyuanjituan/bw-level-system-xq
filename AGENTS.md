# 项目 AI 协作规则

## 通用编码规则

- 默认使用简体中文回答；新增代码注释、错误提示和业务提示使用简体中文。
- 变量、函数、组件、类型、第三方库和 API 名称保持英文。
- 保持现有移动端主题变量、组件风格和响应式布局，不用硬编码测试数据替代可用的接口数据。
- 工作区可能包含用户未提交的修改，只处理当前任务涉及的文件，不覆盖或回退无关变更。

## 组件与样式规则

- 页面应优先复用项目公共组件，不要重复实现已有能力；复制功能统一使用 `src/components/Common/Copy.vue`。
- 公共组件的业务状态色由组件自身维护。页面传给公共组件的自定义 class 默认只调整间距、尺寸和布局，不得用同一个选择器覆盖普通态、成功态等不同状态的颜色。
- `Copy` 组件普通复制图标使用主题主色，复制成功图标必须使用 `--skin__accent_1` 绿色；页面不得将成功态覆盖为 `--skin__primary`。
- 页面使用的静态图片必须下载并存放在项目本地资源目录；用户头像、帖子图片、游戏封面等接口动态资源继续使用接口返回地址，不得下载后写死。
- SVG 图标统一放入 `src/icons/svg` 并通过 `svg-icon` 使用，业务页面不得保留内联 `<svg>` 或直接依赖远程 SVG 地址。
- 推广页样式必须维护在 `src/views/home/promote` 对应组件内，不得依赖 `public` 下打包生成的 CSS，也不得继续使用其中带 hash 的类名作为功能样式依赖。
- 参考外部或 `public` CSS 还原界面时，应将所需规则迁移到语义化的本地 class，并保持现有主题变量、状态色和组件内部样式职责不变。

## 广场业务与界面规则

- 广场相关页面位于 `src/views/home/plazza` 和 `src/views/home/plazza.vue`，资料、统计、最近游戏和动态列表必须使用真实接口数据，不得使用硬编码示例数据替代。
- `/home/plazza/publisher/:id` 的最近游戏区域按参考页面独立实现，不使用 `PlazzaRecentGames` 组件；无最近游戏时不渲染整个区域。
- `/home/plazza` 的“我的主页”需要同时展示本人资料、最近游戏和本人动态，并支持刷新、搜索和游标分页；无本人动态时显示明确空状态。
- 当前用户查看自己的主页或自己的动态时，不显示关注按钮，也不显示头像旁的关注加号。
- 广场下拉刷新和加载更多统一复用 `/home/plazza` 的交互、文案和加载样式，禁止各页面使用不一致的临时加载效果。
- 关注、点赞、帖子收藏和游戏收藏必须调用对应接口，并提供点击反馈、请求中禁用、乐观更新和失败回滚；分享优先调用系统分享，无法使用时回退为复制链接。
- 布尔状态接口必须使用 JSON 请求体，避免表单编码将布尔值转换为字符串后导致关注、点赞或收藏状态异常。
- 使用 TypeORM `orIgnore()` 插入关联记录时必须关闭实体回写，避免无主键返回时触发 `Cannot update entity because entity id is not set in the entity.`。
- 个人资料头像为空或加载失败时统一显示项目本地默认头像，不得使用远程测试头像作为默认值。

## 代理推广业务规则

- 推广页位于 `src/views/home/promote`，代理配置数据统一请求 `POST /app/v1/agent/promoteInfo`。
- 代理模式固定为：`1` 一代净盈利、`2` 两代净盈利、`3` 三代净盈利、`4` 无限代（流水模式）、`5` 无限极差；无限代不能显示为净盈利模式。
- 接口的 `config` 用于展示代理模式、稽核倍数、结算周期、下次结算时间、有效人数门槛和最高返佣比例。
- 下次结算时间由后端计算，前端只负责格式化和倒计时：日结为次日 `00:00`，周结为下一个周日 `00:00`，月结为下个月 1 日 `00:00`。
- 接口的 `ruleGroups` 已按比例规则分类：净盈利模式按代理层级分组，无限代和无限极差按 `gameClassify` 游戏分类分组，前端不得自行重新推断分类。
- 二代比例按一代实际返佣金额计算，三代比例按二代实际返佣金额计算。例如投注 `1000`，一代 `10%` 得 `100`，二代 `30%` 得 `30`，三代 `30%` 得 `9`。
- 推广域名使用接口返回的 `inviteLinks`，其来源已排除官方链接和回家网址；不要在前端写死域名。链接使用 `dl` 参数携带邀请码。
- 多个推广域名允许用户切换，并根据当前链接动态生成二维码；无可用域名时显示明确空状态。
- 金额、稽核倍数和返佣比例统一展示两位小数；接口失败时保留空状态并使用项目统一错误提示。

## 构建规则

- 默认禁止运行任何项目构建命令，包括但不限于 `npm run build`、`pnpm run build`、`yarn build` 和 `vite build`。
- 只有当用户在当前任务中明确要求执行构建时，才允许运行构建命令。

## 界面预览规则

- 默认禁止启动开发服务器或使用浏览器、截图等方式预览界面。
- 只有当用户在当前任务中明确要求预览界面时，才允许进行界面预览。

## ESLint 规则

- 默认禁止运行 ESLint，包括但不限于 `eslint`、`npm run lint`、`pnpm run lint` 和 `yarn lint`。
- 只有当用户在当前任务中明确要求使用 ESLint 时，才允许运行相关命令。
